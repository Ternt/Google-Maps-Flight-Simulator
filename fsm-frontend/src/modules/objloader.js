'use strict';

let OBJLoader = function() {
	
	/**
	 * Load an .obj file and parses it
	 * @param {string} path - a path to the .obj file
	 * @return {object} model - a buffer containing all parsed 
	 */
	let loadOBJ = function(path) {
				
	}
	
	return ({
		loadOBJ: loadOBJ,
	});
}();

export default OBJLoader;
